# Backend API Configuration
VITE_API_URL=http://localhost:8000
VITE_WS_URL=localhost:8000

# Enable API integration (set to 'true' when backend is running)
VITE_USE_BACKEND=true

# Dev mode (uses X-Dev-User-Id header)
VITE_DEV_MODE=true
